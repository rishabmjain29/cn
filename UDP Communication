Server:
import socket

server = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
server.bind(("localhost", 8888))

print("UDP Server is running...")

data, addr = server.recvfrom(1024)
print("Client:", data.decode())

server.sendto("Message received via UDP".encode(), addr)
output:
UDP Server is running...
Client: Hello Server, this is UDP Client



client:
import socket

client = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

client.sendto("Hello Server, this is UDP Client".encode(), ("localhost", 8888))
data, addr = client.recvfrom(1024)
print(data.decode())

client.close()
output:
Message received via UDP

